# Пункт 7. Матрицы линейных отображений и преобразований

Пусть $\varphi : L \longrightarrow L'$, в $L$ задан базис $e_1, \dots, e_n$, в $L'$ — $f_1, \dots, f_m$. Тогда:
$$x \in L \quad x = x_1 e_1 + \dots + x_n e_n \Longrightarrow$$
$$\varphi(x) = \varphi(x_1 e_1 + \dots + x_n e_n) = x_1\varphi(e_1) + \dots + x_n\varphi(e_n) =$$
$$= x_1 \begin{pmatrix} a_{11} \\ \dots \\ a_{m1} \end{pmatrix} + \dots + x_n \begin{pmatrix} a_{1n} \\ \dots \\ a_{mn} \end{pmatrix} = \begin{pmatrix} a_{11} & \dots & a_{1n} \\ \dots & \dots & \dots \\ a_{m1} & \dots & a_{mn} \end{pmatrix} \begin{pmatrix} x_1 \\ \dots \\ x_n \end{pmatrix} = Ax$$
где $A$ — матрица отображения.

Для линейного преобразования $\varphi : L \longrightarrow L$ в базисе $e$ имеем $y = Ax$. При переходе к новому базису $e \longrightarrow e'$ с матрицей перехода $S$ ($e' = e \cdot S$):
$$A' = S^{-1}AS$$

### Обратное преобразование

> [!error] **Теорема.**
> Линейное преобразование $\varphi$ пространства $\mathbb{R}^n$ взаимно однозначно тогда и только тогда, когда его матрица в каком-нибудь базисе невырожденна.

**Доказательство:**
$\square$ [Необходимость $\Rightarrow$]:
Пусть в некотором базисе преобразование $\varphi$ имеет матрицу
$$A = \begin{pmatrix}
a_{11} & \dots & a_{1n} \\
\vdots & \ddots & \vdots \\
a_{n1} & \dots & a_{nn}
\end{pmatrix}.$$
Тогда координаты вектора $\varphi(x) = (\eta_1, \dots, \eta_n)$ следующим образом выражаются через координаты вектора $x = (\zeta_1, \dots, \zeta_n)$:
$$\begin{cases}
\eta_1 = a_{11}\zeta_1 + \dots + a_{1n}\zeta_n, \\
\dots\dots\dots\dots\dots\dots\dots\dots\dots\dots\dots , \\
\eta_n = a_{n1}\zeta_1 + \dots + a_{nn}\zeta_n.
\end{cases} \quad (1)$$

Взаимная однозначность преобразования $\varphi$ означает, что для любого набора чисел $\eta_1, \dots, \eta_n$ найдется ровно один набор чисел $\zeta_1, \dots, \zeta_n$, удовлетворяющих системе уравнений (1). Но система уравнений (1) имеет единственное решение относительно неизвестных $\zeta_1, \dots, \zeta_n$ тогда и только тогда, когда ее определитель отличен от нуля, то есть матрица $A$ невырожденна. $\blacksquare$

> [!info] **Определение.**
> Линейное преобразование $\varphi$ пространства $\mathbb{R}^n$ называется обратимым (или невырожденным), если существует такое линейное преобразование $\psi$, что
> $$\psi\varphi = \varphi\psi = \varepsilon, \quad (2)$$
> где $\varepsilon$ — тождественное преобразование.

Очевидно, что если какое-либо преобразование $\psi$ удовлетворяет равенствам (2), то оно единственно, линейно и невырожденно. Такое преобразование называется обратным для $\varphi$ и обозначается через $\varphi^{-1}$, так что
$$\varphi^{-1}\varphi = \varphi\varphi^{-1} = \varepsilon \quad (3)$$

Ясно, что $\varphi$ обратимо тогда и только тогда, когда оно взаимно однозначно. При этом, поскольку кольцо линейных преобразований изоморфно кольцу матриц, то равенствам (3) будут соответствовать матричные равенства
$$BA = AB = E,$$
где $B$ — матрица линейного преобразования $\varphi^{-1}$ в том же базисе, что и $A$ для $\varphi$. Отсюда как раз видно, что $B = A^{-1}$.
