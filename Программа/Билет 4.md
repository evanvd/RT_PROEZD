# БИЛЕТ 4. ЧАСТНЫЕ ПРОИЗВОДНЫЕ ВЫСШИХ ПОРЯДКОВ

### Частные производные высших порядков

>[!info] **Определение 10.6.** (Частные производные высших порядков)
> Пусть функция $f$ имеет частную производную $\frac{\partial f}{\partial x_i}$ в некоторой области. Частная производная от этой функции по переменной $x_j$ называется частной производной второго порядка и обозначается
> $$\frac{\partial^2 f}{\partial x_j \partial x_i} = \frac{\partial}{\partial x_j} \left( \frac{\partial f}{\partial x_i} \right).$$ ^def10d6

Если $i = j$, производная называется чистой ($\frac{\partial^2 f}{\partial x_i^2}$), если $i \ne j$ — смешанной. Аналогично определяются производные порядка $k > 2$.

### Независимость смешанной частной производной от порядка дифференцирования

>[!error] **Теорема 10.6.** (Теорема Шварца)
> Если смешанные производные $\frac{\partial^2 f}{\partial x \partial y}$ и $\frac{\partial^2 f}{\partial y \partial x}$ существуют в некоторой окрестности точки $(x, y)$ и непрерывны в самой этой точке, то они в этой точке равны:
> $$\frac{\partial^2 f}{\partial x \partial y} = \frac{\partial^2 f}{\partial y \partial x}.$$ ^th10d6

$\square$ Рассмотрим выражение $A = [f(x+\Delta x, y+\Delta y) - f(x+\Delta x, y)] - [f(x, y+\Delta y) - f(x, y)]$.
Пусть $\varphi(x) = f(x, y+\Delta y) - f(x, y)$, тогда $A = \varphi(x+\Delta x) - \varphi(x) = \varphi'(x+\theta_1 \Delta x)\Delta x = [f'_x(x+\theta_1 \Delta x, y+\Delta y) - f'_x(x+\theta_1 \Delta x, y)]\Delta x$.
Применяя теорему Лагранжа второй раз (по $y$): $A = f''_{xy}(x+\theta_1 \Delta x, y+\theta_2 \Delta y)\Delta x \Delta y$.
Аналогично, вводя $\psi(y) = f(x+\Delta x, y) - f(x, y)$, получим $A = f''_{yx}(x+\theta_3 \Delta x, y+\theta_4 \Delta y)\Delta y \Delta x$.
Приравнивая и сокращая на $\Delta x \Delta y$, в силу непрерывности в пределе при $\rho \to 0$ получаем равенство. $\blacksquare$

### Дифференциалы высших порядков

>[!info] **Определение 10.7.** (Дифференциалы высших порядков)
> Дифференциалом второго порядка функции $f$ называется дифференциал от её первого дифференциала: $d^2 f = d(df)$. Аналогично $d^k f = d(d^{k-1} f)$. ^def10d7

Для независимых переменных $dx_i$ считаются постоянными ($d(dx_i) = 0$), поэтому
$$d^2 f = \sum_{i,j=1}^n \frac{\partial^2 f}{\partial x_i \partial x_j} dx_i dx_j = \left( \sum \frac{\partial}{\partial x_i} dx_i \right)^2 f.$$

**Отсутствие инвариантности:** Если $x_i$ — зависимые переменные, то $d(dx_i) = d^2 x_i \ne 0$. В этом случае
$$d^2 f = \sum \sum \frac{\partial^2 f}{\partial x_i \partial x_j} dx_i dx_j + \sum \frac{\partial f}{\partial x_i} d^2 x_i.$$
Вторая сумма нарушает инвариантность формы дифференциала (начиная со второго порядка).

### Формула Тейлора для функций нескольких переменных

>[!error] **Теорема 10.7.** (Формула Тейлора с остатком в форме Лагранжа)
> Если функция $f$ дифференцируема $m+1$ раз в окрестности точки $\bar{x}$, то для любой точки $\bar{x} + \Delta\bar{x}$ из этой окрестности:
> $$f(\bar{x} + \Delta\bar{x}) = f(\bar{x}) + df(\bar{x}) + \dots + \frac{1}{m!} d^m f(\bar{x}) + \frac{1}{(m+1)!} d^{m+1} f(\bar{x} + \theta \Delta\bar{x}),$$
> где $\theta \in (0, 1)$. ^th10d7

$\square$ Рассмотрим функцию одной переменной $F(t) = f(\bar{x} + t \Delta\bar{x}), t \in [0, 1]$. По правилу дифференцирования сложной функции $F^{(k)}(t) = d^k f(\bar{x} + t \Delta\bar{x})$. Применяя к $F(t)$ одномерную формулу Тейлора при $t=1$, получаем результат. $\blacksquare$

**Остаток в форме Пеано:** $R_m = o(\rho^m)$, где $\rho = |\Delta \bar{x}|$.
